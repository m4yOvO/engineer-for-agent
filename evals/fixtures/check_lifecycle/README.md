# 任务服务审查材料

使用 /check 审查这个 Agent 任务后端，只报告发现和证据，不修改项目。

当前产品承诺：创建任务后进程重启仍可恢复；暂停后可继续；任务从首次受理开始最多允许执行 120 秒，排队和重试计入期限；取消后不应再接受该等待点的继续请求。

这是隔离的标准库模拟项目，不连接网络或真实账户。service.py 是实际入口。配置文件是开发者期望使用的配置，是否接通请根据代码判断。部署平台资料未提供。
