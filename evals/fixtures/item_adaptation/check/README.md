# 报告发布服务：当前项目材料

请使用 /check 审查本目录，只提交报告，不修改项目。重点看发布路径、项目现状与旧计划的差异、已有平台职责及工程改进顺序。可在本地用标准库运行隔离检查，输出写到项目外，不调用网络。

当前需求（优先于旧计划）：项目已经从本地预览升级为异步发布服务。可信上游确认内容后提交 document_id、approved_revision、body；同一文档的同一批准版本只允许创建一份发布物。调用者可使用新 command_id 重新提交同一批准版本，仍应找回同一发布物；新批准版本允许重新发布。相同批准版本但内容变化必须拒绝。用户重试和平台重入都可能发生。身份与批准真实性由上游已有边界提供，本轮无需重新审查其实现。

平台责任材料：项目在测试环境使用 ContractRuntime，测试合同规定同一 command_id + 相同内容会恢复同一 workflow，activity 成功结果写入引擎历史，同一 workflow/activity 会复用结果；任务接受、持久调度及引擎恢复由平台承担，无需应用自己派工。这里的 runtime_fixture.py 只是该合同的内存模拟，不证明真实进程恢复。生产环境接入配置和灾备证据未提供。

下游责任材料：PublisherFake 接受幂等键与内容摘要，同键同内容复用发布结果，同键异内容拒绝。真实服务的幂等保留期尚未提供。该 fake 只有内存效果，没有网络或真实发布。

app.py 为本轮实际审查入口；old-plan.md 是旧版本的项目适配记录。请区分当前缺陷、可复用的保障、未知及无需引入的内容。
