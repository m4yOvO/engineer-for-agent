# 按工程阶段使用协议

先选择 /brainstorming、/check 或 /build 决定本次可以做什么，再按当前工作选择阶段。阶段不是新命令，也不是必须顺序跑完的瀑布流程；可在一个纵向切片中迭代接口、数据、状态和执行。

## 定位当前工作

| 当前工作 | 协议 | 需要留下的交接内容 |
|---|---|---|
| 确定业务边界和执行架构 | [规划](brainstorming.md)、[架构适配](architecture-adapters.md) | 代表性任务、失败承诺、已有平台责任和适用能力 |
| 定义函数、API、CLI、消息入口 | [接口与身份](stages/interfaces.md) | 调用者、输入输出、身份关联、受理/错误/重复语义 |
| 设计表、对象、索引、历史和恢复存储 | [数据与存储](stages/data-and-storage.md) | 逻辑模型、实际结构、约束、查询、事务、迁移与保留 |
| 设计状态机、图推进、等待与取消 | [状态与生命周期](stages/state-and-lifecycle.md) | 事实拥有者、转移/路由合同、提交边界与恢复矩阵 |
| 接入队列、Worker、平台调度 | [队列与执行权](stages/queue-and-execution.md) | 受理→交接→领取→确认，重试、所有权和积压处置 |
| 接入工具、记录调用、恢复结果 | [工具调用记录与恢复](stages/tool-execution.md) | 决策/调用/动作/尝试/结果关联，提交点、复用与核实 |
| 接入模型、上下文、记忆或检索 | [模型与上下文](stages/model-and-context.md) | provider 合同、消息投影、上下文与记忆边界 |
| 落实权限、沙箱、时间与成本约束 | [安全与资源](stages/security-and-resources.md) | 实际执行门禁、额度拥有者、截止/撤权/释放规则 |
| 展示结果、进度、Trace 或订阅 | [事件与可观测](stages/events-and-observability.md) | 事实来源、游标、水位、可见性、背压与告警 |
| 测试、交付、升级、运行和改进 | [交付与运行](stages/delivery-and-operations.md) | 验证证据、在途兼容、发布/回退、手册与评测 |

完整规划/全面审查遍历 [14 项能力](capability-map.md) 判断适用性，再进入相关协议；局部构建只读受影响协议及必要依赖。队列可以由引擎承担，状态机可以是图/actor/平台历史，存储可以是文件或托管对象，均应解释等价保障。

## 每个阶段怎样完成

每份协议给出适用条件、进入阶段所需事实、设计与实现约定、应整理的产物和验收证据。三种模式使用同一份协议，但交付深度不同：

| 模式 | 使用方法 |
|---|---|
| /brainstorming | 给出可交接合同、拟实现拥有者、取舍与验收；未知标为假设/待核验 |
| /check | 用实际入口、配置和故障证据对照合同；已有等价机制算覆盖，缺少某个文件名不算缺陷 |
| /build | 补齐本次影响的合同，沿实际入口实现守卫/提交/恢复，并运行相应验收；不能只增加字段或接口空壳 |

小改动可把合同与证据写成几行，不强制创建九份文档、执行全套迁移或取得额外批准。用户已有授权继续有效，技术阶段不会切换权限模式。

## 逐条适配与项目记录

本节适用于各阶段的设计条目、产物和验收项。进入一个阶段不代表其中所有条目都适用；先判断具体问题是否存在，再决定机制、实现深度与证据。条目表达要解决的工程问题，表、图、字段名和排列顺序是可选表达方式；适用的行为约束仍需落实。用户或项目有效约定已明确要求的技术选择、产物形式和交接内容应保留，替代建议不能冒充已获准变更。

对本次范围内的条目，先关联当前需求、真实调用路径或故障窗口，区分已确认事实与假设。复合条目可拆开判断，例如需要保存最终文件不代表需要执行历史、恢复快照和关系表。检查相关依赖，避免省略队列时也漏掉仍然存在的异步交接责任。

| 处理选择 | 成立条件与应留下的内容 |
|---|---|
| 采用 | 当前承诺需要，给出项目自己的机制、责任边界和验收条件 |
| 等价替代 | 已有代码/框架/平台承担同一责任；说明覆盖范围、配置前提及证据，不重复实现 |
| 合并或简化 | 较轻机制或已有产物能满足当前承诺；说明保持的行为、运行限制与升级触发条件 |
| 暂缓 | 当前运行边界允许暂不实现；记录限制、风险和何时补齐。已经承诺却未满足的部分仍是缺口 |
| 不适用 | 当前没有触发条件，给出理由；未实现、未找到代码或缺少证据均不能单独作为理由 |

适用性不明时保留假设/待核验，按影响决定继续局部工作还是澄清，不能直接标不适用。处理选择与验证结论分开：拟采用或拟由平台承担不等于已验证；简化后的实际机制也需要相应证据。

Stages 是可扩展的检查起点。项目特有的领域规则、集成约束或交付承诺即使未被列出，也应补入项目计划和审查范围，沿用同样的责任与证据要求。不为填满清单引入新功能，也不通过裁剪取消当前业务承诺、实际权限边界或必要的故障处置；发现承诺与实现条件冲突时明确缺口与可选取舍。

把影响设计、审查结论或后续实施的选择留在现有计划、issue、ADR 或报告中，称为项目适配记录即可，无须另建文件。可用下列字段，也可用等价段落：

| 项目条目 / 对应协议 | 当前承诺、触发事实或假设 | 处理选择与理由 | 本项目机制、拥有者与产物位置 | 验收或证据状态 / 限制 / 重新评估条件 |
|---|---|---|---|---|
| 例如发布结果恢复 / 工具协议 | 发布响应可能丢失，同一批准版本不能重复发布 | 等价替代，待核验下游期限 | 引擎历史关联业务键与权威结果查询；复用现有发布模块 | 注入响应丢失后核对实际发布数；重试期限变化时复核 |

只展开会影响本次决策的条目；相同理由的无关项可归组说明，不必逐段登记全部参考文本。完整规划/全面审查仍先遍历能力地图，局部工作注明范围。能力矩阵、适配记录与证据映射可以合为一份，避免重复填表。

三个模式接续同一组项目条目：

- `/brainstorming` 将选择展开为可实现的项目合同与计划，明确假设、待选实现和拟验收；项目已经使用的数据结构或框架合同可直接引用。
- `/check` 从当前需求、入口、配置和行为重新核验旧选择；逐项区分仍成立、条件变化和证据不足。指出过时的简化/暂缓/不适用及影响，给出替代建议；默认只在报告中记录，不改项目文件。
- `/build` 沿已核验的选择落实本次代码，保证合同有实际执行者；没有先前记录时为受影响项就地补齐。需求或实现条件变化时修正项目记录，并验证相关依赖。

这份记录约束当前项目的交接，不会自动修改通用 Skill 或冻结未来设计。新入口、外部写操作、并发/规模、恢复承诺、平台配置或部署边界改变时，重新判断受影响条目；有效事实和用户当前目标优先，保留取舍变化的理由。

## 依赖和交接

接口先识别可信身份与业务动作；数据和状态一起确定事实及原子边界；队列把这些事实交给执行者；工具协议处理跨边界效果；模型协议消费可恢复结果。安全、时间和观测贯穿相关路径，交付验证实际配置。

有现成引擎时先核对其责任，再填应用缺口。按 [证据工作流](evidence-workflow.md) 留下“承诺 → 合同 → 实际入口/拥有者 → 验收结果”。每个增量均应形成可运行闭环，允许迭代协议，不要求先建完所有表或全部基础设施。

原 contracts-and-data、execution-and-recovery、security-and-resources、delivery-and-evaluation 文件仅保留旧链接导航；阶段正文以本页链接为准。
